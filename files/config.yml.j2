{% set wc_config_keys = [
    'wc_compose_name',
    'wc_compose_service_base',
    'wc_traefik_admin_host',
] %}
{{ dict(wc_config_keys | zip(q('ansible.builtin.vars', *wc_config_keys))) | to_nice_yaml }}
