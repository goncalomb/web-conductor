- name: Install web-conductor
  hosts: all

  tasks:
  - name: Install pipx
    ansible.builtin.apt:
      name: pipx
      cache_valid_time: 3600
    become: true

  - name: Copy files
    ansible.posix.synchronize:
      src: files/
      dest: "{{ wc_dir }}/"
